# Linked List Project for Data Structures &amp; Algorithms (Spring 2024) 

A basic Linked List program, simply run it and from there CLI should be self explanatory enough

## Potential Improvements

- Reorganize interface code structure
- Rewrite user interface to be more convenient (GUI?)
- Save length value instead of traversing list every time
- Don't use eval() (unsafe)
- More and better commenting
- Unit tests?
